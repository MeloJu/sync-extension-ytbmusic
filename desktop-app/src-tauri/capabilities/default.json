{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability para as janelas locais (settings e panel)",
  "windows": ["settings", "panel"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://music.youtube.com/*" }]
    }
  ]
}
